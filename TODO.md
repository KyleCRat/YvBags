## Future Appearance Settings

- Make the shared accent color customizable, including header text, header dividers, category text, category dividers, and matching hover/pressed accents.

## Agent Updates
- Make the agents file remind us to check after each patch if they added new mouseover visual changes so we can update our instant mouseover re-implemenation to match if they did.

## Currency "Tracked on backpack"
- If a currency is "Tracked on backback" we should dispaly it somehow
  - Floating icons off to the left or right of the bag?
  - Virtual Currency rows in a "Currency" category?
  - Add a scrollarea footer space between the actual scrolarea and the footer that lists tracked currencies?
